function solution(arr) {
    var total = 0;
    for(let i=0;i<arr.length;i++){
        total+=arr[i];
    }
    return total;
};

`function solution(arr) {\n var total = 0;\nfor(let i=0;i<arr.length;i++){\ntotal+=arr[i];\n}\nreturn total;\n};`